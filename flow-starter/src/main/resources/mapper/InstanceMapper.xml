<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.flow.mapper.InstanceMapper">

    <select id="getProcessInstanceStatus" resultType="string">
        select status from flow_instance where id = #{processInstanceId}
    </select>
</mapper>
